name: exercise1

on:
    push:
        branches:
            - main
jobs:
    running_program:
        runs-on: ubuntu-latest
        steps:
            - name: greeting
              run: echo "hello how are you"
            - name: list env
              run: printenv