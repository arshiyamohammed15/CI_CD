name: exercise4

on:
    pull_request:
        branches:
            - practise
jobs:
    exercise4:
        runs-on: ubuntu-latest
        steps:
            - name: greeting
              run: echo "thanks for the pull request"
            